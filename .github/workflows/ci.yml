name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    
    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 . --config=.flake8
    
    - name: Check code formatting with black
      run: |
        pip install black
        black --check .
    
    - name: Run type checking with mypy
      run: |
        pip install mypy
        mypy .
    
    - name: Run basic tests
      run: |
        python -m pytest tests/test_basic.py -v
        
    - name: Install dependencies for full test suite
      if: matrix.python-version == '3.11'  # Only run full tests on one Python version
      run: |
        pip install -r requirements.txt
        pip install scikit-learn
        
    - name: Run full test suite
      if: matrix.python-version == '3.11'  # Only run full tests on one Python version
      run: |
        python -m pytest tests/ -v
    
    - name: Upload coverage to Codecov
      if: github.event_name != 'pull_request'  # Only run on push to main
      uses: codecov/codecov-action@v3
      with:
        token: ${{ secrets.CODECOV_TOKEN || 'dummy_token' }}
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false
        verbose: true
